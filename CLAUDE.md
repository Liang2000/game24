# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**game24** — a fresh, unbuilt scaffold. The repo currently contains only a README (`# game24`) and a Git remote; there is no source code, build system, or test suite yet.

## Repository

- Remote: `https://github.com/Liang2000/game24.git`
- Default branch: `main`

## Development

No build, lint, or test commands exist yet — there is no `package.json`, lockfile, or tooling configuration present. When the first build/test setup lands, record its commands here.

## Agent skills

### Issue tracker

Issues and specs live as GitHub Issues in `Liang2000/game24`, driven by the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
